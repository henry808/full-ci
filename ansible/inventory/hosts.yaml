all:
  hosts:
    webserver1:
      ansible_host: "ec2-35-86-107-31.us-west-2.compute.amazonaws.com"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "~/.ssh/gitlab072723.pem"

  # prod, test, dev go here
  children:
    prod_webservers:
      hosts:
        webserver1:
      vars:
        env: prod
